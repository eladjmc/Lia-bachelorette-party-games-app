# Branding assets

- `bottle.png` — top-down bottle, transparent, pointing right, ~600×180 (Truth or Dare)

App icons / link previews live in `public/` root (`favicon*`, `android-chrome-*`, `og-image.png`) — not on the login screen.
