# Avatar images

We use **12** princess portraits:

| File | Avatar ID |
| --- | --- |
| `1.png` … `12.png` | `princess-01` … `princess-12` |

`13.png` is unused for now (optional spare).

Logical IDs live in `src/features/entry/avatars.ts` and `backend/src/data/avatars.ts` — not the filename.
